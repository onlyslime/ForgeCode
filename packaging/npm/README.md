# @onlyslime/forgecode

Install the ForgeCode terminal agent without Python or uv:

```sh
npm install -g @onlyslime/forgecode
fcc
```

The package selects the prebuilt binary for the current operating system and
architecture. Configure a provider inside the chat with `/login`, then enter
`URL`, `ID`, and `KEY`.
