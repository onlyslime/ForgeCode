# Copy to .env locally if desired. Never put real credentials here.
FORGECODE_MODEL=
FORGECODE_API_KEY=
FORGECODE_BASE_URL=https://api.openai.com/v1
# Optional ignored configuration lives at .forgecode/config.toml. Never put a
# literal api_key/token/password in TOML; only set api_key_env to an env name.
