# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Environments and package manager state
.venv/
venv/
env/
ENV/
.python-version

# Local configuration and credentials
.env
.env.*
*.pem
*.key
*.secret

# Agent runtime data
.forgecode/
logs/
*.jsonl
*.log
sessions/

# Persistent goal prompts (kept locally, never committed)
docs/goals/*

# Local strategy and status notes (kept locally, never committed)
docs/strategy/

# Historical release roadmaps and acceptance reports (kept locally, never committed)
docs/releases/

# Assessment material is local-only and excluded from public releases
docs/assignment/

# OS and editor files
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/settings.example.json

# Build artifacts and temporary files
dist/
build/
tmp/
temp/
*.tmp
*.swp

# Local source audits and vendor clones (never part of the ForgeCode package)
.opencode-src/
opencode-audit/
